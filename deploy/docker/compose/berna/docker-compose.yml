version: '3'
services:
  lazycache:
    build: ../../
    ports:
      - "8080:8080"
    volumes:
      - /data:/srv/overlay:ro
    environment:
      LAZYCACHE_FILEOVERLAY: /srv/data
      LAZYCACHE_FILEOVERLAY_FLATTEN: 1
