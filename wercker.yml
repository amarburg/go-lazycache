box: amarburg/golang-ffmpeg:wheezy-1.8

build:
  steps:
    - setup-go-workspace:
        package-dir: github.com/amarburg/go-lazycache

    # Gets the dependencies
    - script:
        name: go get
        code: |
          go get -t .

    # Build the project
    - script:
        name: go build
        code: |
          go get github.com/amarburg/go-lazycache/cmd/lazycache-server/
          go install github.com/amarburg/go-lazycache/cmd/lazycache-server/
          go build

    # Test the project
    - script:
        name: go test
        code: |
          go test 

trigger-deploy:
  ## Triggers the lazycache-deploy/build pipeline
  steps:
    - canopytax/trigger-build@0.5.3:
      token: $WERCKER_BEARER_TOKEN
      pipelineId: 58813bb5bb0e020100e9525e
      message: Build triggered by deploy-lazycache commit $WERCKER_GIT_COMMIT
