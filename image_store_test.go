package main

import "testing"

import "strings"
import "bytes"
import "fmt"
//import "os"


func TestImageStore( t *testing.T ) {

  // const testString = "abcdefghijklmnopqrstuvwxyz"
  // data := strings.NewReader( testString )
  //
  // DefaultImageStore.Store( "foo", data )
  //
  // reader,err := DefaultImageStore.Retrieve("foo" )
  // if err != nil {
  //   panic(fmt.Sprintf("Error retrieving from bucket: %s", err.Error()))
  // }
  //
  // buf := new(bytes.Buffer)
  // buf.ReadFrom(reader)
  // response := buf.String()
  //
  // if testString != response {
  //   t.Errorf("Response not the same as original \"%s\" != \"%s\"", data, response )
  // }
}
